# Stretch interaction

Place the one-shot stretching sprite sequence here as `000.webp`, `001.webp`, and so on.

Register the files under `interactions.stretch.frames` in `pets/sena/pet.json`. The current procedural stretch remains the fallback while this list is empty.
