# Daydream interaction

Place the one-shot idle/daydream sprite sequence here.

Use `000.webp`, `001.webp`, ... and register them under `interactions.daydream.frames` in `pets/sena/pet.json`. Both the current “发呆” and quiet-companion autonomous events can use this slot.
